# 有预算限制的短期演示部署计划

**目标：**在国内平台验证公网完整流程，最多使用现有 5 元余额，不充值、不升级数据库。未完成部署与验证前不宣称已上线。

**架构：**复用已通过测试的 Next.js、Prisma 和 PostgreSQL 容器。数据库只使用工作空间内网；应用经平台 HTTPS 入口公开。资源以项目标签和固定名称隔离。拟定演示窗口 6 小时，必须先验证到期释放应用、数据库和磁盘的能力，并核算所有计费资源。

**约束：**定时清理和估算不是平台消费硬上限；若不能落实用户的预算限制，不创建持续计费资源。

- [x] 确认 Sealos 杭州账号余额 5.00、工作空间当前无应用和 PVC。
- [x] 下载官方 Kubeconfig，验证集群访问及删除本项目资源所需权限；不提交凭据。
- [x] 重新执行容器验证；镜像已发布 GHCR，CI 离线导出另有成功记录，本机未下载离线归档。
- [x] 准备可供集群拉取的、与测试提交对应的应用和迁移镜像。
- [x] 验证到期资源释放及成本边界；测试只能针对本项目创建的临时资源。
- [x] 部署 PostgreSQL、执行 Prisma 迁移、配置应用与 HTTPS 来源。
- [x] 验证公网填写、恢复、免费字段、模拟支付、重复支付及已支付会话。
- [x] 导出虚构演示数据备份至本地，记录链接有效期；测试 Job 自动释放已验证，业务 Job 尚在运行。
- [x] 更新交付材料和源码归档。
